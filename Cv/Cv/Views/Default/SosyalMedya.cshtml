@using Cv.Models.Entity
@model List<SosyalMedya>


<div class="social-icons">
    @foreach (var x in Model)
    {
        <a class="social-icon" href="@x.Link"><i class="fab @x.İkon"></i></a>
    }
</div>